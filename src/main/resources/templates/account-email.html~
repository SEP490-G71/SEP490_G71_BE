<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <style>
        body {
            margin: 0;
            background-color: #f2f4f6;
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
            color: #2f2f2f;
            font-size: 16px;
            line-height: 1.6;
        }

        .main {
            max-width: 640px;
            margin: 50px auto;
            background: #ffffff;
            border-radius: 12px;
            overflow: hidden;
            box-shadow: 0 6px 20px rgba(0, 0, 0, 0.08);
            border: 1px solid #e3e8ee;
        }

        .top-bar {
            background-color: #0d47a1;
            padding: 24px 32px;
            color: #ffffff;
            text-align: center;
        }

        .top-bar h1 {
            margin: 0;
            font-size: 24px;
        }

        .content {
            padding: 32px;
        }

        .content h2 {
            margin-top: 0;
            color: #0d47a1;
            font-size: 20px;
        }

        .info-box {
            background-color: #f3f7ff;
            padding: 20px 24px;
            border: 1px solid #c2d9ff;
            border-radius: 8px;
            margin: 24px 0;
            font-size: 18px;
            font-weight: bold;
        }

        .info-row {
            display: flex;
            align-items: center;
            gap: 8px; /* khoảng cách giữa label và value */
            margin: 10px 0;
            font-size: 18px;
        }

        .info-label {
            color: #0d47a1;
            font-weight: 700;
            margin-right: 12px;
        }

        .info-value {
            color: #1a237e;
            font-weight: 700;
        }

        .warning {
            color: #c62828;
            font-size: 15px;
            font-style: italic;
        }

        .btn-container {
            text-align: center;
            margin-top: 32px;
        }

        .button {
            background-color: #1976d2;
            color: #ffffff !important;
            text-decoration: none;
            padding: 12px 30px;
            border-radius: 6px;
            font-weight: 600;
            font-size: 16px;
            display: inline-block;
        }

        .footer {
            text-align: center;
            font-size: 12px;
            color: #888;
            padding: 20px;
            background-color: #f8f9fa;
            border-top: 1px solid #e0e0e0;
        }

        @media (max-width: 480px) {
            .content {
                padding: 20px;
            }

            .info-row {
                flex-direction: column;
                align-items: flex-start;
            }

            .info-value {
                margin-top: 4px;
            }
        }
    </style>
</head>
<body>
<div class="main">
    <div class="top-bar">
        <h1>Thông tin tài khoản Medsoft</h1>
    </div>
    <div class="content">
        <h2>Xin chào {{name}},</h2>
        <p>Bạn đã được khởi tạo tài khoản trên hệ thống <strong>Medsoft - Hệ thống quản lý bệnh viện</strong>.</p>

        <div class="info-box">
            <div class="info-row">
                <span class="info-label">Tên đăng nhập:  </span>
                <span class="info-value">{{username}}</span>
            </div>
            <div class="info-row">
                <span class="info-label">Mật khẩu tạm thời:  </span>
                <span class="info-value">{{password}}</span>
            </div>
        </div>

        <p class="warning">⚠️ Vui lòng đăng nhập và đổi mật khẩu ngay để đảm bảo bảo mật tài khoản của bạn.</p>

        <div class="btn-container">
            <a href="{{url}}" class="button" target="_blank">Truy cập hệ thống</a>
        </div>

        <p>Nếu bạn cần hỗ trợ, vui lòng liên hệ quản trị viên hoặc phòng CNTT.</p>

        <p>Trân trọng,<br /><strong>Đội ngũ Medsoft</strong></p>
    </div>
    <div class="footer">
        © 2025 Medsoft. Email được gửi tự động, vui lòng không trả lời.
    </div>
</div>
</body>
</html>
